import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Create Web3 App",
  description: "Generated by create web3 app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
